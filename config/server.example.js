export default {
  // Server port (default: 4317)
  // port: 4317,

  // Google Weather API key for higher-accuracy weather.
  // Without this, the backend uses the free Open-Meteo API.
  // googleWeatherApiKey: "",

  // Direct-link URL for the rotating image tile.
  // Without this, a random placeholder from picsum.photos is used.
  // imageFeedUrl: "https://picsum.photos/1200/800",
};
